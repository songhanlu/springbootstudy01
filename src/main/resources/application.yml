server:
  port: 10013
  tomcat:
    uri-encoding: utf-8

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sb_study001?characterEncoding=utf8
    username: root
    password: 1234
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      - hibernate:
          dialect: org.hibernate.dialect.MySQL5Dialect